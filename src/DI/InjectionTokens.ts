export namespace InjectionTokens {
  // Symbols are used when you want to register a dependency in the container but when the dependency is not a class instance
  export const Token = Symbol("Token");
}
